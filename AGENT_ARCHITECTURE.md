# 序幕 TV Agent 架构

## 目标

Agent 只处理需要编剧判断、上下文取舍和局部修订的工作。项目状态、版本、检查点、并发、重试、网关切换和导出仍由确定性代码管理。

```text
确定性工作流内核
状态 / CAS / 检查点 / 调度 / 重试 / 模型路由 / 结构校验
                         |
受约束 Agent
总编剧 / 路线图统筹 / 分集编剧 / 连续性编辑 / 制片统筹
                         |
工具层
读取最小上下文 / 生成候选 / 规则检查 / 局部修订 / 保存版本
```

## 已落地

### Agent 运行时

位置：`backend/app/modules/agent_runtime/`

每次运行具备：

- 工具白名单。
- 最大步骤数。
- 最大模型工具调用数；工具内部已有的结构修复仍遵守原服务自己的有界上限。
- 每个工具的类型、状态和耗时记录。
- 只记录错误类型，不保存提示词、模型原文或 API Key。
- Agent 不能直接访问数据库；持久化必须通过现有服务和检查点入口。

### 持久化与恢复

`agent_runs` 保存一次单集 Agent 请求的脱敏状态，`agent_steps` 保存每个工具步骤的状态、耗时和可选的已校验产物检查点。数据库中不保存提示词、原始模型响应或 API Key。

同一次前端自动重试复用稳定的 `agent_request_id`：

- 已完成请求直接返回已校验结果，不重复调用模型。
- 前一步已完成而后一步失败时，从该工具检查点继续。
- 相同请求标识对应不同输入时拒绝恢复，防止串用旧结果。
- 服务异常退出后，过期运行会增加 attempt 并继续；原始开始时间保持不变。
- 项目永久删除或重建下游内容时，同步清理所属 Agent 运行记录。

运行状态只能通过项目范围内的 `GET /story-projects/{project_id}/agent-runs/{run_id}` 查询，也可以在同一路径下按集数列出。正式合并到总项目时，由宿主项目的身份认证与项目权限中间件保护该路径；Agent 层还会核对运行记录的 `project_id`，避免跨项目读取。普通创作界面只显示友好的生成阶段和持续时间，不展示工具名或内部错误。

### EpisodeRoadmapAgent

路线图前端现在使用：

```text
POST /story-projects/{project}/plan-nodes/{node}/episode-plans/{episode}/agent-run
```

单次 Agent 运行：

```text
draft_episode_roadmap
    -> inspect_episode_roadmap
    -> [必要时] repair_episode_roadmap
    -> [必要时] inspect_repaired_episode_roadmap
```

它仍然以单集为最小恢复单元，前序已接受路线图仍由前端传入并保存。旧的 `/draft` 接口保留兼容。

### EpisodeScriptAgent

正文已经接入统一 Agent 边界，内部成熟流程不变，但现在会在已校验的 DeepSeek 初稿之后持久化断点：

```text
正文初稿
    -> JSON/结构修复
    -> 漫剧格式检查
    -> 连续性检查与局部修订
    -> 保存已校验初稿断点
    -> GPT 后处理
    -> 剧本质检
```

如果 GPT 后处理或后续网络请求失败，同一 `agent_request_id` 会从初稿断点续跑，不再重复调用 DeepSeek。Agent 层只负责运行范围和统一记录，不增加额外模型调用。

## 后续 Agent

### StoryShowrunnerAgent

读取批准的 Story Bible、剧情树节点和阶段边界，决定当前节点需要补充的剧情职责。它不能修改版本或跳过用户确认；拆分和父子谱系仍由 `story-tree-expansion` 与后端版本服务控制。

### ContinuityEditorAgent

读取上一集退出状态、本集路线图和当前正文，只输出局部修订任务，例如人物状态冲突、伏笔未兑现或结尾交接缺失。它不能重新规划整集。

### ProductionCoordinatorAgent

在正文完成后补全场景、人物频率、道具和人物道具关系的缺失说明。Excel、DOCX 和 ZIP 结构仍由确定性导出代码生成。

## 不交给 Agent 的职责

- 数据库写入、CAS 冲突和版本递增。
- 任务暂停、恢复、取消和并发槽位。
- 网关选择、超时、429/5xx 分类和自动重试。
- 集数边界、8–12 集叶节点、1–5 场景、25–35 句台词等硬规则。
- 用户批准、编辑确认和永久删除。
- 导出文件的列名、工作表名称和文件结构。

## 设计约束

1. 每个 Agent 只处理一个明确主题和最小工作单元。
2. 每次模型调用前先读取最小必要上下文，不把整个工作区重复塞入提示词。
3. Agent 最多执行有限步骤，失败后交还外层恢复机制。
4. Agent 只能生成候选或局部补丁，最终版本由现有检查点和确认流程落盘。
5. 所有 Agent 日志必须脱敏，不向用户展示原始模型诊断。
