"use client";

export function LanguageToggle(_props: { compact?: boolean }) {
  // The creator-facing workspace is always Simplified Chinese. Market choice
  // changes generation culture and delivery language, not the authoring UI.
  return null;
}
