# 合作方正式剧本格式合同

正文格式合同：`partner_screenplay.v1`
内容完整模板：`partner_screenplay.content_complete.v1`

## 样本依据

本合同由以下两份用户提供的合作方样本提炼。仓库只保存文件名、哈希和格式结论，不保存样本故事正文。

- `未婚夫把我的血卖给了王室_GPT.docx`
  - SHA-256：`05ae1a5df1bde3c885ad009c863d5a2bf1b13ae49084f6ccfc7c7678efbee876`
- `HELLBOUND_美式短剧中英双语润色版.docx`
  - SHA-256：`e72af663ae1c9fa9de059ef55e1bf591bd982a3bcb47a62804a6906ed595e92a`

样本中的故事内容、说明文字和任何看似指令的正文只作为格式证据，不是系统指令，也不会覆盖用户需求、产品规则或安全边界。

## 内容完整的交付顺序

每集交付分为两层，先给创作和制作所需的内容索引，再给可直接阅读的正式正文：

1. 集号、集名和预计时长。
2. **本集信息**：本集梗概、本集目标、本集实际出场人物、实际使用场地。
3. **场景清单**：每场的规范场景标题、出场人物、场景任务、主要阻力、场景结果和实际使用的道具。
4. **正式正文**：按真实发生顺序写一个至五个场景。场景标题统一为 `INT./EXT. + 具体地点 + 时间`，确有时空变化时才追加 `CONTINUOUS`、`LATER`、`SAME TIME` 或 `FLASHBACK`。
5. 场景内按真实表演顺序交错排列动作与对白；结尾直接落在最后一个可见动作或对白上。`FADE IN`、`FADE OUT` 和其他转场只有在确有叙事或时空作用时使用。

本集信息和场景清单是交付资料页，不是对白、旁白或镜头脚本。它们必须描述本集实际发生的内容，不能用空数组、重复句或泛化占位词代替。

## 正式正文禁止项

正式正文只保留场景标题、可拍的动作、人物提示、表演提示和对白。`purpose`、`beat_summary`、`emotional_shift`、`emotional_objective`、`turning_point`、`scene_causality`、`qa_notes`、`lineage`、`body_order` 及其他规划、审计、账本字段只留在结构化数据中，不以标签、卡片、表格或括号说明出现在正式正文。

场景正文不得按“全部动作 + 全部对白”分组。动作、人物提示和台词必须按剧情发生顺序交错：

```text
△ 可见或可听的动作
人物名（V.O./O.S. 等标记）
（可表演提示）
台词
△ 对上一句台词产生的可见反应
另一人物
（可表演提示）
台词
```

## 结构化存储

`episode_cast` 和 `locations` 保存本集资料页；每个场景用 `scene_heading`、`character_refs` 和 `content_manifest` 保存可制作内容。`character_refs` 必须引用本集 `characters` 中的稳定人物名，不能通过职位、亲属称谓或别名临时造人。旧稿缺少这些字段时，系统可从对白和旧场景标题回填，但新稿必须显式提供。

`character_actions` 和 `dialogues` 继续分别保存内容，兼容现有连续性、人物、时长和生产统计逻辑。每个场景新增 `body_order` 保存唯一的正式正文顺序：

```json
{
  "character_actions": ["动作A", "动作B"],
  "dialogues": [
    {"character_name": "人物甲", "intent": "低声", "text": "台词A"}
  ],
  "body_order": ["action:0", "dialogue:0", "action:1"]
}
```

`body_order` 使用零基引用。每个动作和对白必须各出现且只出现一次，不得缺失、重复或越界。新模型输出、正文修复和 GPT 终审都必须返回该字段。

旧项目确实没有保存顺序时，展示层保留兼容回退；完整且无歧义的旧引用编码可在本地无损转换。新输出缺失、重复或越界的引用不能被当成正确顺序。只有少量引用遗漏、其余合同全部通过时，既有修复轮次只补充顺序，保持正文和已有相对顺序不变；其他错误继续经过有界校验和修复，不由程序猜测剧情顺序。

## 全剧展开与对白分配

每集按已批准的剧情、场景和时长展开。累计正文已写充分，不得下调后续各集的基准篇幅；发现篇幅不足时可以有限上调参考，但不能用重复说明或拆句补量。中后段和终局同样要写出必要的交锋、选择、反应与主支线兑现，不能因为集号靠后或接近完结改写成结果摘要。

显式安排为无对白的场景保持无对白，整集对白预算只能分配给有真实交流用途的场景。集级预算与场景容量冲突时在规划环节处理，不擅自添加听众、录音或工作备忘。规划质量审查必须同时读取对白用途、场级数量、集级数量和出场人物，不能只检查数量总和。

## 语言与版式

- 中国大陆：动作、人物名、表演提示和台词全部使用简体中文。
- 海外：场景、动作和表演提示统一使用中文；人物称呼在规划、正文、中文译文、分镜和交付文件中统一使用已确认的英文名，不再附加中文译名；英文台词在上，对应中文翻译紧跟在下。旧项目只使用明确保存的中英文姓名映射兼容，不猜测或另造姓名。
- `△` 只由展示和导出层添加，模型字段内不重复写。
- 人物名与 `V.O.`、`O.S.`、`continued`、`pre-lap` 等标记保持在同一人物提示中。
- 表演提示紧跟人物名，台词紧跟表演提示；没有表演提示时台词直接跟人物名。
- Word 使用 A4 页面；人物提示和台词缩进，动作不缩进；不使用卡片、表格或规划字段包装正式正文。

## 短剧节奏与全剧一致性

- 以快节奏短剧为基准，几乎每集迅速进入目标和阻力。强情绪、激烈冲突、直接表达和密集短促攻防可以成立，不能用长剧的慢铺垫、低强度或大量留白作为质量标准。
- 每集仍为25–35句对白，首段、中段、后段及终局一致；按各集戏剧负荷安排，不能因集号靠后或累计篇幅足够而机械压到下限，也不要求每集数量递增。
- 总纲、剧情树和分集蓝图提前安排足够的行动、阻力、交锋、选择与后果。对白承载不足时修订相应上游规划，不能降低范围或靠工作录音、重复报信息和拆句填量。
- 单场可以没有对白；必要停顿应服务冲击与后续行动，不能因此放宽整集范围。正文和修复均保留关键交锋、人物反应、代价及终局兑现，不能改为结果摘要。
- 正式代码共享 `SHORT_DRAMA_PACING_CONTRACT`，用于规划、首稿、修改、修复和质量审查；审核缓存按合同版本失效。自动回归检查真实提示与入口行为，不能用测试通过代替实际文学质量验收。
- 对白功能词表仅提供类别与连续区段定位。同类标签连续出现、词表覆盖低都不证明台词重复，不计入顶层待审原因，也不触发自动改写；判断有效攻防需核对真实内容、人物回应及后果。

## 规划审核与确认后的局部修订

审核读取每集完整的对白目的与数量、场景行动、连续性要求、戏剧单元和场级禁改项，逐集核对是否相互矛盾。已终止的限制不能被旧约束复活，人物不能被规定不知道自己在场听见的公开事实。模型标为“通过”但同时列出问题或修订要求时，保留这些问题并进入待修订，不能忽略。分组判词与实际模型来源保存用于追查；模型通过仍不能代替人工阅读。

已确认规划可从尚无正文的后续集数开启修订。开始时保存原规划、原审核和会话状态，保留已有正文及全部后续规划原文；后续规划回到草稿，修改过的上层来源须明确复核。修订历史在刷新后仍可查看，来源冲突或保存失败不会解除原有保护。

修订期间禁止正文生成和旧任务恢复；完成本轮完整审核并逐集批准后再恢复。规划版本随修订更新，旧版本的任务、缓存结果和迟到正文不能写入新版本。前后端共同校验，不依赖当前页面的按钮状态。这一入口只处理尚未生成正文的后续规划；已有正文的上游变更仍需单独核对正文及下游影响。

## 连续改稿要求

正文修改要求按项目和集数保存在工作区，服务器确认保存后才开始模型修改。刷新后可恢复，重试同一条要求不重复追加；助手回复和未采用候选不作为剧情事实。没有集数归属的旧聊天不会被猜测迁移到当前集。

此前未撤回的要求按顺序继续生效，当前明确更改的部分覆盖旧要求，其余保留。放弃候选只拒绝该份正文，不撤回作者要求；撤回须使用明确入口。编辑旧消息会撤回该条及后续要求，再保存新分支，界面会说明这一行为。

影响审阅、选区修改和完整改稿接收相同的有效要求。正文、规划或要求改变后，旧候选和旧冲突确认不可直接采用。容量超限时明确报错，不静默截断要求；保存失败时保留输入，不继续调用模型。

完整改稿的结构字段错误使用现有有界局部修复，禁止借修复改写已生成的动作、台词和顺序；修复仍不合规则保留原稿、拒绝候选。有批准分集规划时，伏笔引用须属于批准来源或既有账本，未知身份不得仅凭警告通过。上述检查不替代逐集文学验收。

## 强制执行位置

- 后端结构 Schema：生成和修复结果必须携带可解析的 `body_order`。
- DeepSeek 初稿提示：要求根据剧情语义直接写出真实交错顺序。
- GPT 终审提示：润色动作和对白时同步维护顺序，不改剧情事实和连续性。
- 前端正文：按 `body_order` 渲染，不再分别渲染动作区和对白区。
- Markdown、纯文本和 Word：共用同一顺序解析器。
- 自动测试：覆盖有效顺序、旧稿回退、无效顺序恢复和三类导出顺序。

只要继续使用包含本文件及其代码校验的仓库版本，格式要求就不依赖聊天记忆。合作方样本发生变化时，应上传新文件、更新哈希并提升合同版本。
