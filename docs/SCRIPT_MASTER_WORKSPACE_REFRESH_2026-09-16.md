# 剧本大师源码核对与整页工作台

## 上游核对

2026-09-16 权限恢复后拉取 `Xylon2000/project111` 全部远端分支：`final2` 最新为 `dd66d6d8a15f0d01c57e82fd60372b648ff99964`（2026-09-13）；`main`、`final1`、`test1`、`agent`、`feature/cn-mainland-staged-generation` 均没有当前本地分支之外的提交。执行 `git merge origin/final2` 返回 Already up to date。现有生产适配已包含上游功能，以及宿主鉴权/账号隔离/票据续期、灵感输入修复和 v2 批量导入，本次没有重复合并或重写独立后端。发布前再次 fetch 返回 Repository not found，最新核对范围以上述成功拉取的提交为准。

## 界面与往返

- 主站功能栈直接使用已有启动接口，进入完整剧本工作台；取消 iframe、多层嵌入卡片和重复连接标题。离开入口后，迟到的启动响应不会再次跳转。
- 创作设定改为页面内流程，统一中性色和浅绿色强调色。保留三步流程、草稿保存、收起/恢复、自由输入、候选选择与最终生成确认；移除模态背景、强制焦点循环和内部滚动区域。
- 主站清理 83 条已无引用的旧剧本占位/嵌入样式；主站与独立服务仍各自构建，没有复制生成业务。
- 剧本大师及导入回执的“返回主站/返回主项目查看”在新标签页打开，保留当前页创作进度与前端调度。链接携带目标项目，主站只在当前账号可见项目中匹配，再进入该项目剧本页；未知项目退回项目库。
- 独立前端可用 `NEXT_PUBLIC_HOST_HOME_URL` 配置主站根地址，默认生产 `/`、本地 `http://localhost:5173/`。使用不同本地端口时覆盖此公开变量。

## 验证范围

主站前端测试 256/256、lint、生产构建通过。独立前端全量测试 607/607（含返回链接测试）、TypeScript 通过。浏览器使用真实组件及测试接口验证同页进入、空项目首轮问答、选项/保存/收起恢复、批量读取及导入回执、返回正确目标项目。390px 手机页面无横向溢出，创作设定中 iframe 和已打开 dialog 数均为 0。

全仓 `pnpm check` 格式检查通过，仍被既有 `apps/api/src/app.test.ts` 的 5270 > 5267 行数门禁阻断，未放宽基线。本次没有改动后端，不重新宣称历史 Python 全量测试已通过；未调用付费模型或验证百集生产耗时。

## 交付与部署

源仓库拒绝 HTTPS/SSH 推送；主仓库推送预检成功。独立服务源码发布目标是 `usaaron/AIGC_Video_Platform` 的 `codex/script-master-service-20260916` 分支，主站 UI 在 `codex/script-master-refresh-20260916` 分支，保留分开部署。

2026-09-16 01:58（Asia/Shanghai）已部署至 `https://xumutv.com`，仅更新两个 Web 服务，发布记录：

| 项目                     | 已部署版本                                              |
| ------------------------ | ------------------------------------------------------- |
| 主站 Web 源码            | `4abab44841301e3e9758ebb86cc05d5c7b0e001f`              |
| 主站 Web 镜像            | `seqora-web:4abab4484130-workspace`                     |
| 独立 Web 源码            | `5a443223bb0c3ef5134283ba2cb046cb70b65b02`              |
| 独立 Web 镜像            | `script-master-web:5a443223bb0c`                        |
| 保留的 API / Worker 镜像 | `seqora-api:10b50eeb6513-import`                        |
| 保留的独立 API 镜像      | `script-master-api:f3d62e11f9a8`                        |
| 主站源码包目录           | `/opt/seqora-releases/workspace-4abab4484130`           |
| 独立当前源码目录         | `/opt/script-master/releases/5a443223bb0c`              |
| 配置和双库备份           | `/opt/seqora-backups/script-workspace-20260915T175835Z` |

两个 Docker 生产构建通过。发布脚本包含失败自动回滚，备份中保留旧镜像和两个数据库；没有新迁移、模型或 Provider 配置修改。通过容器 ID 对比确认 API、Worker、独立 API 和两个数据库均未重建，主站环境配置文件完全一致。

线上 readiness 正常，未登录访问账号、项目、导入目标、管理端和剧本大师均返回 401。已登录独立页面返回 200；通过业务 API 创建验收项目，导入 1 集、1 镜、3 项分类资产，验证重复回执一致、素材与分集关系正确、人物保持 human、积分不变，最后归档验收项目并退出该冒烟会话。

生产浏览器确认主站同页进入完整剧本大师，桌面和 390px 手机页面均无 iframe、无创作 dialog、无横向溢出；点击“返回主站”在新标签页进入绑定项目的剧本页，原创作页面保留。当前用户已有项目内容未编辑，未触发模型生成。后续仅文档提交不改变以上已部署代码版本。

## 后续修复：恢复左侧项目导航

2026-09-16 02:22（Asia/Shanghai）独立 Web 更新为 `script-master-web:257babe355cf`，源码提交 `257babe355cf59a6d51251e00bc34d994ef887d0`，目录 `/opt/script-master/releases/257babe355cf`；覆盖上表的独立 Web 版本。备份位于 `/opt/seqora-backups/script-sidebar-20260915T182227Z`，主站及所有后端和数据库容器均未重建。

根因是整页打开后，独立服务的旧桌面样式仍隐藏 `ProjectSidebar`。修复后桌面默认展开左侧项目导航，可搜索、新建、切换、删除及进入项目库；顶部按钮可收起或重新展开。手机继续使用默认收起的抽屉。删除重复的顶部项目菜单，统一复用侧栏；内容区按可用宽度布局，展开/收起不重建编辑器。

独立前端 607 项测试、类型检查和生产构建通过。浏览器验证桌面展开/收起、搜索空状态、草稿保留、手机打开/关闭和无横向溢出。线上 readiness、未登录保护、登录后页面、1 集/1 镜/3 项资产导入、重复回执和积分不变检查通过，验收项目已归档。

## 后续修复：明确返回序幕主站

2026-09-16 02:40（Asia/Shanghai）独立 Web 更新为 `script-master-web:066b955f25ff`，源码 `066b955f25ff9df62ee149f64a57ba57bfa3ba55`，备份 `/opt/seqora-backups/script-return-20260915T184030Z`。主站 Web 和所有后端继续使用原版本。

原 Logo 指向独立项目库，主站返回链接不够明显。接入主站时，Logo 改为普通主站链接，左栏顶部、桌面和手机顶部都提供高对比度的“返回序幕主站”，内部首页统一标为“剧本项目库”。返回仍用新标签页保留草稿及调度，界面明确提示这一行为；已绑定项目的链接保留 `projectId` 和 `view=script`，无绑定时返回主站项目库。

607 项独立前端测试、类型检查和生产构建通过，桌面/手机入口及无横向溢出验证通过。线上从剧本项目库返回主站成功，登录保持有效；启动、批量导入、重复回执和积分不变冒烟通过。
