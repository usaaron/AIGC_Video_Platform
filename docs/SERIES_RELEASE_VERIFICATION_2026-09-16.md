# 分集、人物造型与资产库发布验收（2026-09-16）

2026-09-16 07:00（北京时间）发布至 `https://xumutv.com`，07:08 再次确认 readiness、数据库、Redis、队列和 Worker 心跳正常。

## 发布版本

| 部分                  | 代码提交                                   | GitHub 分支                            | 生产镜像                                             |
| --------------------- | ------------------------------------------ | -------------------------------------- | ---------------------------------------------------- |
| 主站 Web、API、Worker | `f0604cdac1eb4b50fc732a3ffcda1cd3bc867147` | `codex/script-master-refresh-20260916` | `seqora-web:f0604cdac1eb`、`seqora-api:f0604cdac1eb` |
| 独立剧本大师 Web      | `e1d32f470720b30bff7ecd56207ebdce187561b1` | `codex/script-master-service-20260916` | `script-master-web:e1d32f470720`                     |

两分支均已推送至 `https://github.com/usaaron/AIGC_Video_Platform`。独立 FastAPI 继续使用 `script-master-api:f3d62e11f9a8`；本次独立服务改动位于 Web 导出接口及其运行配置。

主站 `SCRIPT_MASTER_INTERNAL_URL` 在生产 Compose 中指向 `http://script-master-web:3000/script-master`，独立 Web 运行时的 `BACKEND_API_URL` 指向 `http://script-master-api:8000`。外网登录网关与后端签名票据校验均保留。此次不变更支付、模型密钥或生产环境文件；环境文件备份与发布后副本逐字节一致。

## 备份与发布检查

- 最终备份：`/opt/seqora-backups/series-20260915T230025Z`，含双方数据库 dump、主站 JSON 索引、原源码目录、原镜像记录和私密配置备份。
- 发布代码来自已提交的 Git 归档；主站源包 SHA-256：`11eacdbbaad18529792d4247138f0c8dfbefa755f78232daae32ccb2f1d94892`；独立服务源包：`384c53626fd2761739930841cfa43ae8b7f956d1fc90ecee7cb438236434b428`。
- 服务器未启用 BuildKit，沿用此前发布方式，仅去除候选 Dockerfile 的依赖缓存挂载参数；业务源码及构建命令不变。三个生产镜像均完成构建。
- 迁移通过后才重建主站 API、Worker、Web 和独立 Web。双方数据库、Redis、独立 API 的容器 ID 均保持不变。
- 早一轮验收脚本错误地用 POST 创建独立项目，并遗漏归档的版本参数，触发自动回退。按真实 PUT 和 `expected_revision` 契约修正脚本后，候选容器及最终生产验收通过；首次生成的测试数据已清理。
- 未登录 `/api/v1/auth/me`、`/api/v1/projects`、`/api/v1/library/items`、`/admin/` 和 `/script-master` 均为 401。

## 测试结果

| 流程                                   | 结果                                                                                                     |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 个人测试账号、组织成员、组织管理员登录 | API 均通过；浏览器额外完成既有 `test01@test.invalid` 登录与退出，无强制改密                              |
| 组织权限                               | 普通成员 Admin 访问 403；组织管理员可见本组织 8 个成员                                                   |
| 组织充值                               | 通过受审计管理 API 加 1 积分，成员余额同步可见，再扣回 1 积分；最终共享余额恢复为 0                      |
| 个人积分充值                           | 浏览器选择档位后打开微信／支付宝二维码占位弹窗，金额与档位一致；点击“我已支付”明确提示未扣款、未改变权益 |
| 会员充值                               | 微信／支付宝月度会员占位弹窗均通过；个人测试账号保持免费版和 2235 积分                                   |
| 支付接口边界                           | 生产支付 provider 未配置，checkout 返回 503；前端直接改套餐 403；无签名 Stripe 回调 401；余额未改变      |
| 长素材分集                             | 线上测试账号输入 2750 字，先得到可编辑的两集计划，可改标题、返回原稿；未确认付费生成                     |
| 网剧资产分类                           | 浏览器仅显示人物、场景、物品、品牌和音频，无独立服装选项                                                 |
| 全剧资产去重                           | 两集测试正文中的林晚合并为一个人物、两个造型；重复导入返回同一资产 ID，无重复生成任务                    |
| 主站资产库                             | 已保存剧集自动收录，重复刷新数量不增加；文本可下载，导入目标网剧后确实写入新集草稿；其他账号读取返回 404 |
| 剧本大师资产库                         | 实际独立后端保存两集模拟正文，签名同步成功收录两集和一个合本                                             |
| 历史产物可见                           | 浏览器打开个人测试账号资产库，自动显示 12 项历史产物及版本记录                                           |

发布前主站 `pnpm check`、592 项稳定回归和增量检查通过，独立 Web 610 项测试及构建通过。本次补充生产连接配置后，27 项配置／桥接回归、格式、架构、安全、lint 和 API build 再次通过。桌面和手机的分集／造型流程已在上一轮完成 4 项浏览器回归。

## 测试边界与清理

未调用付费生成模型，未进行实际微信、支付宝或 Stripe 付款。支付页面仍是模拟接口，实际收款、支付回调和自动到账不属于本次验证通过的能力。

本轮测试创建的主站与独立项目均已归档，对应自动库条目移入回收站，避免再次自动收录。测试组织净生成扣费为 0；加减 1 积分的验收记录保留在账本。候选测试容器已移除，最终测试会话已退出，容器中的临时账号交付文件已删除，原有测试账号密码保持不变。
