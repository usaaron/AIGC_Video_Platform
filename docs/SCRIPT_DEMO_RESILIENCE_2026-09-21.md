# 剧本入口、分集浏览与生成恢复发布

2026-09-21 02:16:57（Asia/Shanghai）已发布至 https://xumutv.com。本次只更新配套剧本 API / Web，主站业务代码及运行容器保持原版。

| 服务              | 运行镜像或业务提交                                                  |
| ----------------- | ------------------------------------------------------------------- |
| 剧本 API / Web    | `script-master-api:53364bb946e2` / `script-master-web:53364bb946e2` |
| 剧本完整业务提交  | `53364bb946e2f94d86414f777efb8bb2c6b2ed69`                          |
| 主站 Web          | `seqora-web:2cc67d2b591b`                                           |
| 主站 API / Worker | `seqora-api:ebb55fa3c7a9`                                           |

配套源码：[剧本分支](https://github.com/usaaron/AIGC_Video_Platform/tree/codex/script-master-quick-preview-20260920)。本分支后续仅文档提交不改变运行镜像。完整实现、恢复边界与发布证据见 [配套剧本发布说明](https://github.com/usaaron/AIGC_Video_Platform/blob/codex/script-master-quick-preview-20260920/deploy/DEMO_WORKFLOW_2026-09-21.md)。

## 操作变化

- 新建快速项目直接进入「原始资料」，保存后进入故事梗概；已有进度继续恢复。
- 主项目隐藏「故事正文参考规模」，独立标准剧本大师保留。快速版默认 8 集 / 8000 字，总正文不超过 10000 字。显式改集数时按每集约 1000 字安排并封顶，载入旧稿不改原目标。
- 人物与世界观、分集安排显示接口实际提供的中文阶段、模型过程和耗时。后台步骤在切页后保留进度；返回读取已保存结果，未保存编辑受保护。
- 分集总览 24 集分页，可按集数或标题查找；生成时可浏览已保存集。场景出场人物改成紧凑标签。
- 失败保留已完成成果，缺失集可续跑；断流不盲目重复模型请求。诊断信息只含项目、阶段、请求与保存进度，不含凭据或正文。

后台恢复有时间上限，并不等同于跨服务重启的持久任务队列。关闭或刷新后读取已保存状态；未完成步骤可能需要恢复。真实质量问题仍会停止生成供作者处理，不伪造成稿。

## 验证

前端全量 1409 项、后端组合 246 项、最终镜像真实独立 PostgreSQL 3 项测试通过，TypeScript / OpenAPI 契约及生产构建通过。最终 API 使用正式 UID 10001 导入成功，Web 正式 UID 1000 的 1436 个运行文件可读。首次候选的源码权限问题在部署前被检查拦截并修正，没有切入生产。

独立本地预览以模拟模型完成 8 集生成与检查，验证切到资产页后返回、72 集翻页与第 25/72 集跳转、未统一确认前浏览其他集和人物标签操作。线上样本通过正文交付、5 项资产、2 个规则分镜、制作修订历史、登录与项目隔离、幂等与积分不变检查。

生产浏览器实际新建项目已进入原始资料，显示 8 集 / 8000 字；改为 1 集后自动变为 1000 字，保存进入快速梗概页仍保留该设置。临时项目已在主站与剧本两侧归档，独立验收页关闭，测试 API 会话退出。此次没有付费模型调用，以上不代表真实模型质量或完整付费生成链路已经验收。

主站此次仅补发布文档，格式、差异、架构与部署控制检查通过。完整 `pnpm check` 在隔离工作区的依赖准备阶段未完成并已停止，不计为全链检查通过；本次没有重新构建或发布主站业务。

## 运维

- 发布目录：`/opt/seqora-releases/script-demo-resilience-20260921-final`；`build.exit=0`、`deploy.exit=0`、`smoke.json` 的 `ok=true`。
- 备份：`/opt/seqora-backups/script-demo-resilience-final-20260920T181627324662803Z`；包含经验证的剧本数据库 dump、配置和原源码指向。
- 剧本源码：`/opt/script-master/releases/53364bb946e2`，`current` 指向该目录。
- 切换前两次扫描 13 个 schema 的在途任务和快速租约，并检测 API 在途连接；确认空闲后才切换。
- 主站 Web / API / Worker、两个数据库与 Redis 容器 ID 均保持不变；无迁移、无模型配置修改。
- 回退使用 `script-master-api:e71ee9874823` 和 `script-master-web:e71ee9874823`；先确认无在途任务，仅重建剧本应用，不覆盖数据库。

本次合成预览 5186 / 3196 / 8196 已按精确进程归属停止，SQLite 和测试证据保留。原本地服务未被本次清理操作启动或停止。
