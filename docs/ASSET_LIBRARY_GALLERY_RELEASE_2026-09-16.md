# 资产库画廊发布验收

2026-09-16 11:02（北京时间）已发布到 <https://xumutv.com>。

- 分支：`codex/script-master-refresh-20260916`。
- 业务代码：`0bdab6a3d394862c7a23899fc87bf6e1bfa7cb1a`。
- API / Worker：`seqora-api:0bdab6a3d394`；Web：`seqora-web:0bdab6a3d394`。
- 迁移：`043_asset_library_prompt_template.sql` 已执行。
- 备份：`/opt/seqora-backups/gallery-20260916T030155Z`，包含数据库、JSON 兼容数据、配置、旧源码和镜像记录。
- 独立剧本大师保持原镜像；数据库和 Redis 容器未重建。

## 验证结果

- 在隔离工作树对本次提交运行 `pnpm check`，格式、架构、部署安全、lint、594 项测试及所有应用构建通过。第一次运行有一项测试数据库重置 hook 超时，整套复跑通过。
- 额外显式执行资产库、模板、Agent 路由专项 8 项测试及真实 Postgres 专项 2 项测试，验证分类分页、模板新增与版本、账号隔离、回收站预览和新增 migration。
- `pnpm test:e2e` 的 17 条浏览器回归通过，覆盖主站与管理员端，以及图片大师、分集流程、资产预览、提示词收藏/编辑/恢复、剧本全文和音频播放器。390px 手机布局无横向溢出，弹窗可关闭；一句成片不请求历史任务。
- 本地真实账号保存模板成功；桌面资产卡片、手机预览与筹备页已人工检查。新增文本版本按版本号读取，预览和下载链接携带版本号，避免编辑后读到旧缓存。
- 生产测试账号：登录、项目列表、六类筛选分页、模板创建/修改/历史版本下载/回收站预览/恢复/清理均通过。
- 生产一句成片规划、确认、恢复、重试及跳过阶段均返回 `503 AGENT_STUDIO_UNAVAILABLE`。
- 剧本大师宿主签名入口与项目列表通过。测试前后积分摘要一致，未触发真实付费生成或支付；临时模板记录已删除。
- readiness 的数据库、Redis、队列和 Worker 心跳均正常；未登录访问账号、项目、资产库、管理端和剧本大师均为 401。
- 线上入口及 App、AssetLibraryPage、FunctionStackPage 按需加载资源均返回 200，确认资产库和筹备页新文案已上线。生产浏览器检查覆盖登录页；登录后的桌面与手机交互使用本地浏览器和自动回归验证。

## 发布过程与回滚

首次静态资源校验只检查入口文件，之后漏读 App 中间层，两次均触发自动回滚。修正为入口 → App → 页面资源的完整加载路径，并对候选 Web 镜像离线校验通过后，使用相同业务代码重新发布成功。最后一次生产业务与静态资源校验全部通过。

历史任务和项目数据保留；一句成片页面不再展示制作任务列表。新增模板类型使用原资产库的数据、版本、权限和去重机制。功能范围见 [实现说明](./ASSET_LIBRARY_GALLERY_2026-09-16.md)。
