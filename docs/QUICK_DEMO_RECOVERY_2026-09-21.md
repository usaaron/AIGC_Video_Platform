# 快速剧本超时恢复、助手布局与海外两集

配套剧本业务提交为 `e2d4aa71dd1720ad88643d193d2f5b6932e37ae5`。本次只更新剧本 API 和 Web，主站应用无业务改动。2026-09-21 04:07:07（上海时间）已部署至 https://xumutv.com。

## 变化

- 修复创作安排仍在输出思考时被旧的 120 秒请求截止时间终止。快速版独立设置请求 480 秒、操作 600 秒，只有创作安排使用 `low` 推理，模型与真实思考流保留；标准流程和并行请求保持原配置。
- 原异常被请求预算及模型备用路径包装后丢失原因，现保留类型因果，并区分截止时间、网关、连接、限流、配置和结果格式。失败只暂停当前步骤；明确重试先恢复状态，再单次请求该步骤，不重做成功的梗概或正文。
- 无内容阶段以宽助手为主、资料及已确认梗概为辅；主要按钮跟随当前任务。有结果后恢复正文主栏，供核对编辑。失败明确提供「重新生成创作安排」，详细原因收起。
- 大陆和海外均可使用快速创作；旧海外作品显式输入 2 集后，可保存资料并进入两集快速模式。保留市场、人物身份及表演资料。海外台词逐句保留英文和中文翻译，其他内容使用中文；双语对照保留到编辑、导出和主站交付。

## 验证边界

前端 1432 项、后端相关 281 项测试通过，TypeScript 和生成接口契约检查通过。本地模拟模型完成两集海外生成、逐集检查、全文检查和交付，共 7 次合成操作；每集 25 条英文与 25 条中文译文进入主站。切页、选集和资产衔接通过。

另一个隔离项目注入一次安排超时，调用顺序为 `synopsis → plan:timeout → plan`，证明恢复不重做已确认梗概。1280×720 浏览器确认宽助手主按钮、输入框及发送区完整显示。虚拟时间测试验证超过旧 120 秒可继续，481 秒截止且仍只有一次物理请求；同一适配器并发的标准流程保持原预算。

测试不代替真实模型质量与速度验收，本轮未发起付费模型调用。失败不会伪造成稿；质量检查未通过时保留成果供作者处理。服务重启后的恢复仍以已保存结果为准，不等同于持久化的后台模型队列。

## 生产与运维

| 服务              | 当前镜像                                                            |
| ----------------- | ------------------------------------------------------------------- |
| 剧本 API / Web    | `script-master-api:e2d4aa71dd17` / `script-master-web:e2d4aa71dd17` |
| 主站 Web          | `seqora-web:2cc67d2b591b`                                           |
| 主站 API / Worker | `seqora-api:ebb55fa3c7a9`                                           |

最终镜像的独立 PostgreSQL 16 验证通过：3 项存储 CAS 回归及 1 项海外两集完整 Quick 验证，包含幂等、换池后恢复和双语保存。生产手工双语样本通过权限、设置及人物资料保留、两集交付、5 项资产、4 场规则分镜和修订历史保护，余额及用量不变，测试项目两侧归档并退出测试登录。实际 API 只读配置核对为请求 480 秒、操作 600 秒、安排推理 `low`。

线上浏览器通过临时项目实际输入海外发行与 2 集，确认「按 2 集快速创作」可用，保存资料后宽版助手保留 2 集 / 2000 字及中英对照要求，主按钮和发送区完整显示；未点击模型生成。API 合成资料初版缺少前端所需 `updatedAt`，不属于完整客户端恢复样本，因此不将首次浏览器载入计作恢复通过；该测试辅助资料已补齐，实际用户操作与本地完整恢复分别验证。

发布目录 `/opt/seqora-releases/script-overseas-short-20260921`；构建、独立 PostgreSQL 与部署退出码均为 0。切换前两次门禁确认 13 个 schema 中无在途任务、有效 Quick 租约或非数据库模型连接。主站三服务、两个数据库及 Redis 容器 ID 保持原值，无数据库结构迁移或共享配置修改。

备份 `/opt/seqora-backups/script-overseas-short-20260920T200637389844213Z`；当前剧本源码 `/opt/script-master/releases/e2d4aa71dd17`。回滚先确认无在途任务，再恢复剧本 API `53364bb946e2`、Web `dbb3386baced` 和旧源码指向，不覆盖数据库。本次无需回滚。后续仅文档提交不改变业务镜像。

配套源码与详细说明：[剧本分支](https://github.com/usaaron/AIGC_Video_Platform/tree/codex/script-master-quick-preview-20260920)、[配套发布记录](https://github.com/usaaron/AIGC_Video_Platform/blob/codex/script-master-quick-preview-20260920/deploy/QUICK_DEMO_RECOVERY_2026-09-21.md)。
