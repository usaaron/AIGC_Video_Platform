# 分集路线图篇幅目标

编号：`planning.roadmap_length`。状态：`active_shared`。

来源：[backend/app/modules/script_engine/story_planning_service.py:697](/Users/simonriley/Downloads/docs/backend/app/modules/script_engine/story_planning_service.py:697)。符号：`EPISODE_ROADMAP_LENGTH_TARGET_CONTRACT`。

250-650 字；其中蓝图本地派生一句与当前生产补全路径有差异。

本页保留当前工作区原文，不是优化后的替代提示词，也不是某次模型调用的完整展开结果。

## 长文本阅读视图

按源码位置列出长字符串；静态文本按字符串值显示，花括号内动态表达式仅供识别，未执行。条件分支分别列出，不代表一次调用全部生效。短标签、拼装顺序和完整条件见后面的源码原文。

### 片段 1 · 源码第 697 行

````text
分集路线图篇幅目标（按每集路线图所有人类可见叙述字段合计、中文字符估算）：
- 每集 250-650 字；其中 `synopsis` 单独保持约 120-320 字，必须是可直接给创作者阅读的完整因果梗概；
- 场景执行蓝图由服务根据已验证字段和制作预算本地派生，不在路线图里写成正文。
路线图只承担叶节点的逐集展开：本集目标、进入状态、冲突、主角决定、可见回报、退出状态、结尾钩子和下一集压力；同时明确本集发生在哪些场地、哪些人物出场，以及一段完整梗概。不要重复总纲、剧情树或整段场景 prose。若超过上限，先压缩重复说明，再返回 JSON。
````

## 源码原文

此部分按原始行提取，保留缩进、条件、占位变量和全部短字符串。

````python
EPISODE_ROADMAP_LENGTH_TARGET_CONTRACT = """分集路线图篇幅目标（按每集路线图所有人类可见叙述字段合计、中文字符估算）：
- 每集 250-650 字；其中 `synopsis` 单独保持约 120-320 字，必须是可直接给创作者阅读的完整因果梗概；
- 场景执行蓝图由服务根据已验证字段和制作预算本地派生，不在路线图里写成正文。
路线图只承担叶节点的逐集展开：本集目标、进入状态、冲突、主角决定、可见回报、退出状态、结尾钩子和下一集压力；同时明确本集发生在哪些场地、哪些人物出场，以及一段完整梗概。不要重复总纲、剧情树或整段场景 prose。若超过上限，先压缩重复说明，再返回 JSON。"""
````

片段 SHA-256：`67a8f9a41419e182ce0cfd7686bd86981fd85b60a48e4d8dec0fe7538a0471b1`
