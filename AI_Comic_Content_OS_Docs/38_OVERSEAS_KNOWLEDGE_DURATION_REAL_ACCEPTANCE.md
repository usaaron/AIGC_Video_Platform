# 海外知识状态与时长修复后的真实验收

日期：2026-09-08（Asia/Shanghai）。范围：第 37 号修复后的海外 v2 连续三集有界实测。候选始终为 provisional，未向日常项目写入测试正文。

## 实际结果

900 秒总时限触发，进程组退出 124；4 次物理请求，完成 2/3 集。第一集 332.052 秒、第二集 499.251 秒，第三集已请求但没有完整稿或初稿检查点。三集质量、连续性和成本验收仍未通过，不能进入八集放行。

本次首集的时长从 73 秒经一次编辑到 103 秒，两个人物的时间差均为 known。第 36 号首集则为 61 → 139 → 93 秒、两次编辑。这是新的实际样本改善；相同规划下初稿内容仍有随机差异，单次运行不足以证明修复带来稳定的速度或费用降低。

证据目录：`.cache/real-generation-probe/overseas-three-v2-knowledge-duration-20260908/`。独立重放副本：`.cache/real-generation-probe/overseas-three-v2-knowledge-duration-replay-20260908/`。

## 运行范围

- 沿用已授权最多 6 次物理请求、整轮 900 秒，触限即停；没有在本轮追加生成。
- 海外路径，固定 v2 Story Bible、节点和三集 Episode Plan。除实例 ID 外规划与第 36 号一致，从新第一集开始，未注入第 37 号人工修订稿。
- 隔离 SCRIPT reasoning 为 medium，日常 `.env.local` 未变。实际四次请求均为 `gpt-5.6-sol` medium，含一次非流式编辑；没有触发 SCRIPT_REPAIR。
- 主请求累计截止 600 秒、初稿链路 840 秒、单次 I/O meter 限制 180 秒，外层 watchdog 900 秒。20 个运行源码指纹在测试前后相同。
- 零调用预检、执行参数、原始请求、固定输入及指纹均保留。准备阶段不算生成成功。

## 耗时与用量

| 请求 | 阶段 | 耗时 | 已知输入 / 输出 / 合计 tokens |
| --- | --- | --- | --- |
| 1 | E1 初稿，SSE | 248.028 秒 | 20,270 / 7,299 / 27,569 |
| 2 | E1 局部编辑，非流式 | 83.738 秒 | 9,316 / 2,090 / 11,406 |
| 3 | E2 初稿，SSE | 499.162 秒 | 22,375 / 8,080 / 30,455 |
| 4 | E3 初稿，SSE，中断 | 没有完成耗时 | 未知 |

已知合计输入 51,961、输出 17,469、总量 69,430 tokens，其中缓存输入 0、推理输出 3,934。推理 token 已包含在输出内，不能重复相加。第三集 usage 缺失，整轮总量和金额均未知；不能将其算成零，也不能据正文字符数外推整部长篇费用。

E1 初稿首文字约 64.126 秒，E2 约 67.600 秒。E3 在请求后 6.846 秒收到响应头、6.849 秒收到首字节，产品日志约 21.001 秒出现首文字；最后活动快照为 51.086 秒、1,624 个结构化文本字符，并非完整剧本正文。没有观察到 E3 terminal 或供应商失败响应。前两次 SSE 的 meter `closed` 对应正常结束迭代器，应用已成功保存，不能单凭该词判为传输失败。

| 分集 | 有效正文字符 | 场 / 动作 / 对白对 | 估算时长 | 编辑次数 | 自动连续性 QC |
| --- | --- | --- | --- | --- | --- |
| E1 | 1,613（动作 464，英文对白 1,149） | 3 / 16 / 30 | 73 → 103 秒 | 1 | 0 blocking、0 warning |
| E2 | 1,281（动作 445，英文对白 836） | 3 / 16 / 30 | 77 秒 | 0 | 0 blocking、2 warnings |
| E3 | 无完整稿 | 未验收 | 未验收 | 未验收 | 未验收 |

累计有效正文 2,894 字符，不含中文对白对照的重复计量。E1 本次保存总耗时 332.052 秒、两次请求、38,975 tokens；第 36 号对应为 514.050 秒、三次请求、52,766 tokens。编辑成功标记为 `deferred=false`、`quality_status=passed`，旧 deferred 原因不存在；E2 直接通过编辑契约，保留本次合法的 `validated_source_ready` skip 原因。编辑通过不等于语义通过。

## 首集文本审阅

按第 29 号表阅读 3 场、16 个动作和 30 对对白，结合 intent、body_order、状态与初稿检查点；主代理与独立助手交叉复核。初稿到最终稿只有第 2、3 场改变，第一场、三个 body_order 和所有人物/连续性状态不变。

E1 未确认 P1/P2，确认以下 3 项 P3，样本为 revise。JSON Pointer 使用零基下标，均指 `episode_001/draft.json`。

| 编号 | 证据 | 判断与处置要求 |
| --- | --- | --- |
| S01 / P3 | `/scenes/1/character_actions/2`、`/character_actions/3`、`/dialogues/6/intent` | 手机先滑到末尾 08:00，随后点 20:00，又指向发布时间；须交代切换或同屏布局。初稿已有，编辑保留。对白时间比较本身正确。 |
| S02 / P3 | `/scenes/1/character_actions/4` | 两部手机都展示材料，却“截下两部手机并排核验的画面”；须明确拍摄设备或分别保存方式。初稿已有，编辑增加确认保存仍未澄清。 |
| S03 / P3 | `/scenes/1/dialogues/2` | 编辑新增 Both times matter，译为“这两件事不能同时不重要”，量词和对象偏移；应明确两个时间都重要。未改变主线证据，但不能算逐句一致。 |

保护名单、付款截图仅向亚当展示、两小时材料差异、实际事故与责任人尚未知、钥匙转交伊芙和转运待发生均有正文依据。两人物的时间差 statement 为 known，并进入第二集实际检查点及记忆；“实际事故时间仍未查明”的 known 指向整个命题，并不表示知道事故时间。首集已知知识枚举冲突未重现。

## 第二集与连续性

第二集初稿和最终稿场景完全相同，未运行编辑。已完整阅读 30 对对白、16 个动作、状态及实际第三集请求，样本仍为 revise。

钥匙由伊芙开门后收回，工单原件在仓内归还原箱；封签已开启，状态没有伪装成完整封签。正文明确核对 18:30 停工与通报持续作业至 20:00 的冲突，没有再发现首集付款时间差，也没有把差异等同实际事故时刻。伊芙用姐姐生前号码自报身份并说明新拍工单，诺拉才回复；会面地点与亚当保持距离的安排出现在正文中。

以下 JSON Pointer 指 `episode_002/draft.json`。

| 编号 | 证据 | 判断与处置要求 |
| --- | --- | --- |
| S04 / P2 | `/scenes/2/character_actions/4`、`/character_actions/5` 与 `/character_state_updates/1/knowledge_changes/2`、`/knowledge_states/4` | 亚当已退到停车区另一端，最后一条“不要让科尔家族的人靠近我”才到达伊芙手机，正文没有显示给他看或转述；状态却写他已确认这一具体家族禁令。此前听到“独自赴约”支持他保持距离，但不能代替他获知最后追加的家族针对性警告。须补传递动作，或保留他只知道独自赴约条件。 |
| S05 / P3 | `/scenes/2/character_actions/1`、`/character_actions/2` 与 `/dialogues/5` | 已发送身份和新拍工单说明，随后却说 I'm only telling her who I am / 我只告诉她我是谁。两语一致，但与已发送内容不一致；应准确说明已告知身份和影像存在，未发送附件。正文信息渠道已经建立，不应删去该渠道。 |

S04 不代表亚当确实不可能获知，而是当前正文未建立该确定知识的来源。后续投影和第三集记忆已带入“对诺拉的警告保持警惕”“诺拉明确排斥科尔家族”等状态/变化原因；缺少第三集正文，尚不能判断该状态是否改变后续剧情。这也说明本轮 known/disproved 修复只处理一种冲突，不能替代信息来源审阅。

自动的两条 capability_conflict warning 分别把“不能暴露无辜证人身份”和“不能替伊芙决定公开时间”当成身体能力限制，再用走动、拿钥匙等动作触发。这两条属于规则误报，不能作为人物违反底线或伤病状态的证据；原始 QC 不改写，后续应单独收窄分类并覆盖反例。

第二集状态已投影到 provisional Workspace、保存并读回。第三集实际请求含 through_episode_number=2 的 provisional 检查点，记忆召回 sufficient、missing_requirements=[]。E1→E2 已有真实接续，E2→E3 仅验证请求输入，未有第三集正文，不能宣称三集连续推进通过。

| 第 29 号审阅项 | E1 | E2 | E3 |
| --- | --- | --- | --- |
| 批准计划与结尾义务 | pass | pass：主要选择与会面条件有正文 | not_assessed |
| 证据因果 | pass：仅核实材料差异 | pass：停工/持续作业字段冲突，保留未知 | not_assessed |
| 人物动机与代价 | pass | pass：带走原件的冲突、保留来源和独自赴约 | not_assessed |
| 人物知识和账本 | pass | revise：S04 信息来源缺口 | not_assessed |
| 中英逐句一致 | revise：S03 | pass：S05 是双语共同的正文矛盾 | not_assessed |
| 场景与表演顺序 | revise：S01、S02 | revise：S04、S05 的传播与陈述需要澄清 | not_assessed |
| 后处理语义保持 | revise：新增 S03 | 未执行编辑，初稿场景相同 | not_assessed |
| 三集连续推进 | E1→E2 已有真实接续 | 受 S04 影响，不能全项放行 | not_assessed |

pass 仅表示本次文本审阅未发现该项需阻断的问题。没有专业编导盲评或观众测试；S01–S05 均未在原样本上修改，不能将记录处置要求当成已经解决。

## 重放与现场保护

原始 summary 是第二集保存时的快照：elapsed=834.157 秒、请求数 3、status=preparing，不能代表整轮最终状态。独立 `watchdog_result.json` 记录实际退出 124、4 次请求、2 集完成、1 次未知用量，未覆盖原始日志或数据库。

原始 SQLite 有两个 completed Agent、四个完整检查点、两个 draft/provisional 制品；第三集残留 running 行，但本地进程组已停止。不存在第三集初稿检查点或完整制品，没有把残留 running 误称为活跃后台任务。

复制证据目录后，在独立进程对副本重放 E1/E2：HTTP 200、稳定业务结果相同、模型请求 0。仅排除检查点明确省略的 llm_raw_output、prompt_text、rendered_variables 三个调试字段，完整调试响应并非逐字相同。原始数据库和正文指纹复核不变。

前端实际 `buildEmbeddedOverseasDialogueView` 转换为 E1 62、E2 63 个非空唯一路径，0 warnings、0 网络调用、输入不变。该项是数据转换检查，未做新 DOM 或浏览器验收。

本轮未改产品运行代码或测试脚本，因此未重复全量回归；沿用第 37 号后端 `1134 passed, 1 skipped` 的代码基线。API、前端均 200，5 个日常项目完整响应和 `.env.local` 指纹不变，日常 running Agent 为 0。

| 原始文件 | SHA-256 |
| --- | --- |
| E1 draft.json | `e06e10aa1165018a1811e0e6dabb800165d8cd565a4a5025c65758980bed2755` |
| E2 draft.json | `05e0ecfcbfbe5d7002c7536d3db6708c0ecdd8f1ec5f4187988a45ba5cd1018c` |
| E3 request.json | `b61a79358dcfc22a875bb26aa872363423b6fbf157db82f21e8207a58d95efaf` |
| provider_requests.jsonl | `0861a0dd85c9739c3b3ea6a749e79a7f9260760fc1fb77432f4f1619d1f93c14` |
| probe.db | `853f07410f1abe3677bc0258452224a842e92cb9a6e01754e34f42fc61d51838` |

## 剩余范围

仍有 3 个验收阶段，不是再操作 3 次：

| 阶段 | 当前可核对的完成项 | 退出条件 |
| --- | --- | --- |
| 1. 三集验收 | 本次完整保存从第 36 号的 1/3 推进到 2/3；首集时长一次编辑达标、错误 disproved 未重现；前两集重放通过 | 补齐 E3，修订已定位语义问题，完成三集逐项审阅；P1/P2 清零或有作者裁定，P3 有处置；用量与金额可核对 |
| 2. 八集单元 | 未启动 | 连续八集的证据、人物、局部结算、下一单元交接及故障恢复通过；预算须先明确，当前没有已批准的八集金额或耗时阈值 |
| 3. 生产化 | 认证、租户隔离、预算与无人值守尚未完成 | 完成上述工程项；宣称完整长篇能力还须覆盖产品 8–20 万字档位的真实作品、跨单元连续性、人工质量、恢复、耗时及账单 |

下一项先处理本轮确认的问题，并让隔离探针支持保留来源的续跑：复制已保存结果、校验规划/源码/来源、重建前集状态，只生成缺失部分，每轮仍受明确预算限制。现有 `--verify-replay` 仅重放已完成集，不具备续生成能力。续跑得到的是分轮连续性证据，不能冒充“三集在一次 15 分钟内全部生成”的性能通过；复用/修订前集也必须显式标明，不计为全部新生成样本。

本轮没有实现该续跑功能、没有修改原稿，也没有启动八集或第三集补测。完整作品验收与金额核账仍在未完成范围内，专业编导及海外对白自然度的独立审阅仍待进行。
