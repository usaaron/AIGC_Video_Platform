# 海外修订续跑与第三集补测

日期：2026-09-08（Asia/Shanghai）。承接第 38 号报告，范围为 S01–S05 显式修订、身体能力误报修复、隔离探针续跑和第三集补测。用户指定本轮以补测、审阅和问题报告为终点，新发现的语义问题留待后续处理。

结论：第三集补测完成，332.767 秒、3 次物理请求，三集内容齐备；前两集是明确标记的人工修订稿，第三集为新生成。保存与零调用恢复通过，第三集语义仍为 `revise`，三集创意质量、一致性和连续性尚未通过验收，稳定生成速度也未完成验证，未启动八集。

用户重申的优先级：创意质量、一致性、连续性第一，速度也很重要，不考虑 token 消耗。本文用量数字仅保留为运行记录；token、费用和账单完整性均不是优化目标或验收门槛，不因这些因素阻塞后续工作。速度优化必须保住创作质量、必要信息和连续性校验，并以达到可用质量的端到端耗时衡量。

## 来源与范围

原始样本：`.cache/real-generation-probe/overseas-three-v2-knowledge-duration-20260908/`。该轮在 900 秒停止，完整保存前两集，第三集请求用量未知；已知累计 69,430 tokens。

本次保留原始数据库、正文、请求和中断日志。前两集显式修订并另存 revised/provisional 制品，续跑在新的 SQLite 副本中重建产品检查点和记忆，仅请求缺失的第三集。所有候选仍需作者审阅。

这次证据属于“前两集人工修订 + 第三集新生成”的分轮连续性验证，不能记作全新三集自动质量通过，也不能记作一次 15 分钟内完成三集。历史未知用量按原记录保留，不影响验收或推进判断。

## 本轮修复

- 身体能力警告仅匹配明确身体状态或身体行动限制。证人保护、公开决定权、调查授权等约束不再仅因含有“不能”而触发；仍是非阻断词法提醒，不代表完整语义判断。
- 生成提示要求为每个人物的新知识建立读屏、听见内容或转述依据，并遵守消息到达和离场顺序。
- 生成与编辑提示补充页面切换、单机截图与多设备拍照、提及附件与发送附件、数量词与对象对应约束。提示本身不保证后续生成质量。
- 修订 S01–S03：明确通报页面切换，以现有两部手机完成保存，校正“两个时间”的中文表达。
- 修订 S04–S05：在末尾补上伊芙展示完整警告、亚当读后点头的动作；准确说明已发送身份与工单影像存在的信息，未发送附件。

## 验证记录

生成与编辑提示相关测试 47 项通过；身体能力及连续性相关测试 151 项通过。产品运行代码完成后，后端全量为 `1196 passed, 1 skipped`；续跑、修订、用量及重放验证合入后的后端全量为 `1226 passed, 1 skipped`，证据 `.cache/overseas-resume-final-backend-20260908.xml`。探针最后一次落盘更新后，相邻 45 项再次通过，最终全量复核仍为 `1226 passed, 1 skipped`，见 `.cache/overseas-resume-release-backend-20260908.xml`。仅有测试依赖的 Starlette/httpx 弃用警告。

前端 typecheck 及产品投影、海外恢复相邻测试 10 项通过。本次未改前端，不把上述数据层检查描述成新的浏览器或 DOM 验收。

本地应用已恢复，API 与前端均返回 200，原有 5 个项目的数据库记录在恢复前后相同。

## 修订证据

正式目录：`.cache/real-generation-probe/overseas-three-v2-report38-revised-v3-20260908/`。早期无 v3 和 v2 的目录是未完成调试产物，不用于验收。

E1/E2 各 4 处显式变更，8 处 before/after 和原始/修订文件 SHA-256 位于 `revision.json`。两集 `body_order`、人物状态和连续性状态字段未改；新的正文传信动作支持亚当原有的确定知识。独立助手按演出顺序通读 6 场、32 个动作和 60 对对白，未发现修订新增的明显因果、双语或道具调度缺陷，S01–S05 的定点问题均已处理。

修订脚本相关 18 项测试通过。原始 27 个文件和原始草稿制品保持不变，2 个 revised/provisional 制品经 review-draft 重算、保存和读回一致；当前连续性检查均为 0 blocking、0 warning，原第二集的两条 warning 单独保留。修订过程为 0 次模型请求。

第三集实际请求的 relevant_character_refs 包含三人，但生成的有界检查点仅含伊芙和诺拉。亚当的完整 `nora_condition=known` 在 Workspace 中保留，警告反应和变化原因进入第三集记忆与渲染提示；不能据此声称亚当的具体知识键也在有界检查点中。修订目录中的第三集文件仅是请求，尚不是第三集正文。

## 续跑边界

新增 `--resume-from`、`--revised-dir` 和按文件当前 SHA-256 指定的 `--allow-source-change`。恢复前核对固定规划、原始文件、请求指纹、completed Agent、最终检查点、provisional 制品和 Workspace 内容；不接受断档或有可恢复检查点却被标为缺失的集。

本轮从第 38 号不可变原始目录加正式 v3 修订目录续跑。复制数据库后仅在副本退役第三集残留 Agent，并给新请求分配独立 key。通过产品投影逐集重建前两集，修订稿新建 revised/provisional 制品；本轮新用量与继承的 4 次历史请求分别统计。历史 usage 只部分提供字段时也不能标记总用量完整。

原始稿/新生成稿的 `--verify-replay` 重放已完成检查点，继承修订稿通过制品与 Workspace 读回核对，不重新调用模型，也不把人工修订稿当成原模型检查点。本次不支持把含继承修订稿的续跑目录继续作为任意层级的新来源；可回到原始目录加修订包重新准备缺失集。

零调用预检目录为 `overseas-three-resume-preflight-20260908`，其独立重放副本为 `overseas-three-resume-preflight-replay-20260908`。两者都以 0 次模型请求验证前两集和第三集请求；正式补测目录为 `.cache/real-generation-probe/overseas-third-resumed-20260908/`。

## 补测结果

整轮 332.767 秒，E3 API 生成耗时 329.781 秒，正常退出 0。预算为最多 6 次物理请求、900 秒，本次未触限。只调用第三集，没有重新生成 E1/E2。日常配置未改，正文角色为隔离 medium。

| 请求 | 实际阶段/模型 | 耗时 | 输入 / 输出 / 合计 tokens |
| --- | --- | --- | --- |
| 1 | 初稿 SSE，gpt-5.6-sol medium | 216.653 秒 | 22,755 / 6,236 / 28,991 |
| 2 | 场景编辑，gpt-5.6-sol medium | 105.666 秒 | 10,262 / 2,841 / 13,103 |
| 3 | 场景标题语言窄修复，deepseek-v4-flash medium | 7.172 秒 | 1,121 / 762 / 1,883 |

三次均有 usage：本轮输入 34,138、输出 9,839、合计 43,977 tokens；缓存输入 0，推理输出 1,333 已包含在输出内。第 3 次请求使用实际配置的语言窄修复模型，不能把整轮说成单一模型测试。历史 69,430 + 本轮 43,977 = 113,407 已知 tokens；原第四请求用量仍缺失，累计总量及金额均未知。

E3 初稿 3.158 秒收到响应、16.968 秒出现首文字。初稿估时 79 秒，最终 113 秒；最终 3 场、16 动作、30 对对白，有效正文 1,632 字符，其中动作 396、英文对白 1,236，不重复计中文翻译。修订 E1/E2 分别为 1,655 / 1,367，有效正文三集合计 4,654。

E3 编辑 gate 唯一列出的问题是三个英文场景标题。一次完整场景编辑仍保留原 `body_order` 与所有人物/连续性账本，随后窄修复处理标题；最终 `deferred=false`、`quality_status=passed`，没有将其解释为语义通过。初稿与最终场景共有 106 处字段差异，存于 `episode_003/editorial_changes.json`；初稿已从只读检查点提取为 `pre_edit_draft.json`。

自动连续性为 0 blocking、1 warning，具体为未登记的 `hook_payoff_target_episode_4`。前端三集双语转换分别为 62 / 63 / 63 个唯一非空字段、0 warnings、0 网络调用、输入不变，记录 `bilingual_verification.json`；不是浏览器验收。

## 第三集审阅

主代理按 `body_order` 通读三场、16 个动作、30 对中英对白，核对 E2 修订稿、固定 v2 规划、初稿检查点、人物/连续性/关系/伏笔账本及产品投影。未进行第三集独立编导或母语盲评。

下表 JSON Pointer 均指 `episode_003/draft.json`，零基下标。所有问题均记录待处理，本轮未改 E3。

| 编号/级别 | 证据 | 判断和后续处置 |
| --- | --- | --- |
| N01 / P2 | `/scenes/0/dialogues/8`、`/scenes/0/dialogues/9`、`/scenes/0/character_actions/3`，以及 `/relationship_state_updates/0/change_cause` | E2 约定在马路对面，证人害怕时退更远。E3 改为守候车厅入口，诺拉明确说仍太近，伊芙让她先看证据，没有让亚当退远或建立诺拉接受新距离的依据；账本却写全程遵守距离要求。需执行退远，或由作者明确修订条件及接受过程。初稿已有，编辑保留。 |
| N02 / P2 | `/character_state_updates/1/knowledge_changes/1`、`/character_state_updates/1/knowledge_changes/2`、`/character_state_updates/1/knowledge_states/1`、`/character_state_updates/1/knowledge_states/2` | 亚当在远处只看到安全手势，正文没有传递调度记录线索或姐姐签字；账本却记为 known，还把伊芙姐姐误写成“诺拉姐姐”，并与“未获知…具体内容”并存。错误已进入 E3 保存后的 Workspace。需修正人物归属和知情范围，或补可见传递来源，不能仅靠场号认为知晓。初稿已有，编辑未改账本。 |
| N03 / P3 | `/scenes/0/character_actions/1` 与开场两句；`/scenes/1/dialogues/0`、`/scenes/1/character_actions/1`；`/scenes/2/dialogues/0/intent`、`/scenes/2/dialogues/1/intent`、`/scenes/2/dialogues/3/intent`、`/scenes/2/character_actions/2`、`/scenes/2/character_actions/3` | 亚当先说话再出现；诺拉先询问已遮姓名，动作随后才遮；纸条在 intent 中递出、接过、抄号后，动作又写编号、折纸和交接。第三场合上平板后才保存也需交代重新打开或保存时点。需要统一 intent 与 body_order；主要顺序问题初稿已有、编辑保留。 |
| N04 / P3 | `/scenes/2/dialogues/5/text` | 编辑把原“保护你的身份”扩写为“Nothing will point back to you”，与前场承认无法保证不被追查的边界不一致。中译忠实于扩写后的英文，但保证程度变强，宜收回无依据的绝对保证。 |
| N05 / P3 | `/setup_payoff_updates/0/setup_payoff_ref` | `hook_payoff_target_episode_4` 不在批准规划或既有伏笔账本中，是自动 QC 的真实引用警告。需使用已批准引用，或经规划登记；不能因为它看起来像字段名就当作合法 ref。初稿已有。 |
| N06 / P3 | `/character_state_updates/0/lasting_marks/0`、`/character_state_updates/2/lasting_marks/0` | 将暂时失去公开自由、留下调查线索写入身体持久印记字段，混淆临时约束、调查进展与身体后果。需回归合适账本字段；本次未观察到身体能力阻断。初稿已有。 |

| 验收项 | 本次判断 |
| --- | --- |
| 创意质量 | 尚未完成验收。人物选择、冲突递进、揭示的新意、对白和情绪效果仍需作品层面的审阅；通过格式、保存或规则检查不足以证明故事好看。 |
| 证据因果与三集推进 | 工单签收人核验、18:30 停工、调度记录移交和姐姐生前一周签字均有正文；没有重发现付款差或揭露最终主使。该主线通过本次阅读核对。 |
| 人物动机、代价和批准约定 | revise，N01；不得用保护身份的口头承诺覆盖未执行的距离条件。 |
| 人物知识与后续记忆 | revise，N02、N05、N06；自动 QC 没有覆盖全部确定问题。 |
| 中英逐句对应 | 未确认新的明确对译偏差；N04 是编辑扩大英文承诺，中文同步扩大，不是单独翻译错误。 |
| 场景与表演顺序 | revise，N03。 |
| 后处理语义保持 | revise，N04；账本内容原样保留不能证明账本正确。 |
| 生成速度 | 第三集补测耗时 332.767 秒；这是单次样本，且正文仍需修订，尚不能证明达到可用质量的稳定速度。 |
| 三集质量放行 | 未通过，不进入八集。判断依据为创意质量、一致性和连续性，token 与金额不参与放行。 |

速度观察：初稿剧内估时 79 秒已在允许范围，唯一编辑 gate 是标题语言，仍进行了约 106 秒的整场编辑，再用约 7 秒窄修复标题。后续可验证语言问题先走窄修复能否缩短端到端耗时，并检查是否保住正文创意质量、一致性和连续性；不能仅因少一次调用就判定方案更好。本轮不改变路由，也不预先承诺可省去的时间。

## 保存与复核

原始来源和正式修订包的全部指纹验证不变。补测数据库中 E1/E2/E3 各有 completed Agent，共 6 个完整检查点、3 个 draft/provisional 和 2 个 revised/provisional 制品；旧 E3 的 failed 行只在副本保留，不是活跃任务。新 E3 的 API run、draft、制品和 Workspace 读回一致。

在独立 `.cache/real-generation-probe/overseas-third-resumed-replay-20260908/` 验证：E1/E2 修订制品检索通过，E3 检查点稳定业务结果重放通过，0 次模型请求。E3 仅排除已知的三个检查点调试字段，不宣称完整原始响应逐字相同。`independent_verification.json` 与 `replay_verification.json` 保存核对结果。

补测期间 20 个已记录源码文件未变，包含生成、编辑、模型适配器、连续性和投影；`scripts/run_real_generation_probe.py` 在 03:15:57 落盘更新，运行初始 SHA 为 `ef71bcae2feabfe65b4126019a14ac763640a630efc66f404682f16347693e68`，结束后为 `a560e47fe42447d54004c4b386fcb9fc18e02ed6dc0d71d655ff48aea243fd8f`。生成子进程没有热重载，原始 manifest 保留启动时记录，结束指纹另存，不能声称全部 21 个文件在整轮被冻结；零调用重放使用结束后的探针版本。

| 产物 | SHA-256 |
| --- | --- |
| 第三集 draft.json | `b6d3d291756ea2ec423a259dc7c08d4f0cdfb27b1dcbb60e5e8bc89b8bbceb1b` |
| 补测原始 probe.db | `cf179ae9c24e8da2a213115bde86333ac65f29e6ce1c123289d654367493e6d9` |

下一轮第一项为 N01/N02 及其余局部问题的明确修订，并完成创意质量审阅，再决定是否重测或推进八集；同时评估保住质量前提下的速度改进。本轮已按用户指定停在补测、审阅和报告，不追加模型调用或处理新问题。三集质量、八集单元、生产化仍是未通过的验收阶段；完整 8–20 万字作品的质量、连续性与速度，以及认证、租户隔离和无人值守均不在本次完成范围。账单和 token 消耗不列为后续待办或阻塞项。
