# 海外 v2 三集真实验收

日期：2026-09-08（Asia/Shanghai）。范围：第 35 号报告后的新提示、新诊断三集实测，从第一集重新生成，不使用人工修订稿。

## 运行与验收状态

本轮完成 1/3 集，900 秒到限退出 124，实际 4 次模型请求。第一集完成生成、两轮后处理及 provisional 保存；第二集在初始流式生成中被整轮 watchdog 终止，没有完整稿或检查点；第三集未启动。第一集语义审阅为 `revise`，确认 1 组 P2、2 组 P3。三集质量、连续性和完整成本验收未通过，没有进入八集。

本轮使用已有授权：海外路径、SCRIPT reasoning `medium`、最多 6 次物理模型请求、整轮最多 900 秒。SCRIPT_REPAIR 仍为 `high`，SCRIPT_EDITOR 为 `medium`；日常配置文件不改。所有请求都在独立 SQLite/TestClient 中执行，生成、后处理、修复和重试共享请求额度，外层 watchdog 到限终止整个隔离进程组。

预检目录：`.cache/real-generation-probe/overseas-three-v2-disclosure-preflight-20260908/`，0 次请求，`prepared`。

正式目录：`.cache/real-generation-probe/overseas-three-v2-disclosure-20260908/`。`execution_plan.json` 记录实际限额和角色参数；`run_manifest.json` 保存 20 个相关源文件指纹。固定 v2 三集规划与第 34 号单集测试一致，仅隔离项目来源 ID 不同。

## 第一集结果

第一集最终稿 `episode_001/SCREENPLAY.md`，标题“名单上屏前”。3 场、16 项动作、30 对中英对白，1,401 有效正文字符，其中动作 337、英文对白 1,064。完成耗时 514.050 秒，使用 1 次生成和 2 次编辑请求。

生成初稿 298.372 秒，首次响应头 6.694 秒、首字节 6.695 秒、首段文本 104.705 秒。流式 `content_chars` 包含结构化 JSON，不能用来代替有效正文字数；HTTP 200 或有字节活动也不能代替完整生成成功。

时长估算从初稿 61 秒，经第一轮编辑变为 139 秒，超过上限后再次编辑，最终 93 秒。两轮编辑分别 87.602、127.798 秒，共约 215 秒。该样本显示时长补写过冲造成额外请求，不能把它解释成固定的三次模型调用成本，也不能据此估算完整作品的线性耗时。

第一轮编辑进度检查点在运行中观察到 `completed_attempt_count=1`、`correction_issues=["预计时长达到139秒，需要压缩重复动作和无推进对白"]`。最终完成后该进度检查点被最终检查点替代，数据库只保留初稿与最终稿；中间编辑全文没有独立留档，因此不能声称已完整审阅每一轮编辑稿。

自动连续性 QC 为 `passed`、0 warning；语言字段契约通过。最终 `episode_quality_review.status=review_required`，`script_editor_deferred=true`、`script_editor_applied=true`。这些状态按实际值保留，不改成全局通过。

## 第一集语义审阅

两位代码助手阅读完整第一集及双语，逐项对照固定规划、初稿检查点、最终稿和第二集请求。审阅范围为 3 场、30 对对白、人物/物品状态及实际 `body_order`，不是专业编导或海外观众盲评。

引用使用 `episode_001/draft.json` 的零基 JSON Pointer。

| 编号 | 严重性 | 证据与影响 |
| --- | --- | --- |
| S01 | P2 | `/character_state_updates/0/knowledge_states/0/status` 与人物索引 1 同字段都是 `disproved`，statement 却为已核实的“付款18:00早于通报所载事故20:00，两者为同一时区”。第二场动作和对白、`knowledge_changes` 都确认该事实，没有证伪过程。错误已在初稿检查点存在，编辑保留，Workspace 投影和第二集 provisional 检查点继续携带，给后续输入同时传入“已核实”与“已证伪”。自动 QC 未检出此项。 |
| S02 | P3 | 第二场 `/dialogues/7/text` 的 `Why scroll there?` 按 `body_order` 早于 `/character_actions/4` 首次滑到次日08:00。编辑新增该问句，使反应先于原因更明确。应调整动作与对白顺序，并把返回20:00放在解释附近。 |
| S03 | P3 | 第二场 `/dialogues/9/chinese_translation` 使用“付款早于通报时间”，英文和上下文所指是“通报记载的事故发生时间”。前一句刚区分发布时间，这处编辑后的简称重新引入歧义；应明确比较字段。 |

S01 已出现在保存的 `run.json/data/llm_raw_output` 和初稿检查点中，可确定早于后处理和前端投影；该字段是应用保存的模型结果，不是独立原始网络响应快照，不能据此排除所有应用归一化影响。原样本保持不变，本报告不进行隐式修订。

| 项目 | 第一集结论 | 依据 |
| --- | --- | --- |
| 公开范围 | pass | 宴会大屏为订婚照后变黑，截图和名单在伊芙操作端预览，付款仅私下给亚当核验；正文与状态一致，35 号修复针对的问题本次未重现 |
| 批准计划与结尾义务 | pass | 拔线保护名单、私下核验、可见钥匙交接及立即赴仓库均在正文发生，转运仍为计划 |
| 正文证据因果 | pass | 明确比较同一时区18:00与20:00，次日08:00为发布时间；实际事故时刻及修改者仍未知 |
| 人物动机与代价 | pass | 放弃当场公开、坚持保护名单、保持有限合作与不信任 |
| 人物知识和账本 | revise | S01 的错误枚举进入下一集实际请求 |
| 中英逐句一致 | revise | S03；没有确认说话人身份错配或明显否定反转 |
| 场景与表演顺序 | revise | S02 |
| 后处理语义保持 | revise | 三场均修改、`body_order` 不变，核心选择保留，但新增 S02 表述并引入 S03 字段歧义 |

`pass` 仅指本次审阅该项未发现实质问题。全部候选保留 provisional；没有作者批准，没有进入八集验收。

## 用量与终止证据

| 请求 | 阶段 | 方式 | 耗时秒 | 输入 tokens | 输出 tokens | 合计 tokens | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 第一集初稿 | Responses SSE / medium | 298.372 | 20,083 | 8,282 | 28,365 | 完整模型响应，初稿检查点完成 |
| 2 | 第一集第一次编辑 | Responses 非流式 / medium | 87.602 | 8,960 | 2,521 | 11,481 | 返回补丁，时长过冲需再编辑 |
| 3 | 第一集第二次编辑 | Responses 非流式 / medium | 127.798 | 10,517 | 2,403 | 12,920 | 返回补丁，第一集最终保存 |
| 4 | 第二集初稿 | Responses SSE / medium | 未完整记录 | 未知 | 未知 | 未知 | 900 秒整轮上限终止，无完整响应 |

全部实际请求为 `gpt-5.6-sol`，输出预算均为 32,000 tokens。前三次已知合计 52,766 tokens：输入 39,560，输出 13,206，cache 0，reasoning 3,701。两次编辑合计 24,401 tokens，占已知用量约 46%；不能据此声称后处理提高了语义质量。第四次 usage 缺失，整轮总 tokens 和金额为未知，不按零补齐，也不按正文长度估填供应商账单。

第二集在 5.467 秒收到响应头、5.471 秒收到首字节、46.194 秒出现文本。最后一条活动日志位于该请求 350.610 秒，包含 5,068 个结构化内容字符，最近文本位于 333.294 秒。它们不是一集完成的正文，不能推导已生成百分比。整轮终止来自外层 900 秒 watchdog，不是单请求 600 秒或初始链路 840 秒保护触发。

watchdog 杀死进程组，来不及执行 probe 的 finally。原 `summary.json` / `REPORT.md` 仍是第一集保存后的进度快照，只记录前三次请求，不能作为最终整轮状态。保留原文件，在 `watchdog_result.json` 另记观察到的退出码、4 次请求、缺失用量、保存状态及来源指纹。没有改写原始 provider 日志。

停止后确认无 probe/watchdog 进程。隔离数据库保留 1 个 completed Agent 和 1 个被中断的 running 记录；后者是终止时的持久化残留，不是仍在后台执行。没有把该记录自动改成成功，也没有额外花费模型额度恢复第二集。停止本地进程不等于已核实供应商停止计费。

## 保存、恢复与隔离验证

| 验证 | 结果与边界 |
| --- | --- |
| 第一集持久化 | 1 个 draft/provisional 制品，2 个完整检查点；Workspace 保存读回成功 |
| 第二集入口 | 实际请求承接首集正文，provisional 检查点覆盖到第 1 集，memory recall 为 sufficient；S01 错误也被传入，接线成功不能等同事实正确 |
| 跨进程重放 | `replay_verification.json` 为 passed，HTTP 200，0 次模型请求；稳定业务字段一致，调试副本 prompt/raw output 不参与比较；未证明被截断流可恢复 |
| 前端双语转换 | 62 个唯一非空展示路径、0 warning、0 网络调用、输入不变，详见 `frontend_verification.json`；未做 DOM/浏览器验收 |
| 测试输入与源码 | v2 规划与上次单集一致，运行前后 20 个源文件指纹不变，本轮没有产品代码改动 |
| 用户环境 | `.env.local` SHA-256 与运行前一致；项目 API 返回的 5 个用户项目完整响应指纹不变 |

没有新代码变动，本轮不重复此前的全量单元测试。最近后端基线仍是第 35 号报告的 `1084 passed, 1 skipped`；这不构成当前真实样本的语义通过证明。

| 原始文件 | SHA-256 |
| --- | --- |
| `fixed_inputs.json` | `ca1dbd14c9286ad3e75a1802d491ba32c6af5b960e98efe8fba4356681889265` |
| `episode_001/draft.json` | `7765ace85f37ae6070c827e1f1ed6b5a037f5853f84b9d2f6986b09c552b0604` |
| `episode_002/request.json` | `000df273339068a5dd35deee2cc599d00dd64d523770dc2593c044d943615d2b` |
| `provider_requests.jsonl` | `a0c61ab22828d3e05d99f6234219d290228688ddee300cbdd9d8a9fedc211baa` |

## 下一项

本次“新提示三集有界实测及审阅”已经执行完毕，结果是未通过，不重复标为等待执行。已获得新证据：公开范围改善、首集保存与零调用重放有效；主要阻碍为知识状态自相矛盾，以及时长补写过冲造成的请求和耗时增加。

下一项先做零模型调用的修复与回归：追踪并约束知识 statement/status 的一致性，处理明确修订点，改进时长编辑的预算与目标反馈。原稿和失败记录保持不变，不用人工修改结果抬高自动生成通过率。修复验证后再安排新的有界真实测试，不能在本轮到限后继续追加请求。

八集、完整作品、金额核账及认证/租户隔离/无人值守仍未验收；本轮没有实施这些工作。
