# 旧项目两集快速创作入口

旧标准作品原始资料页的集数校验为 8–2000。输入 2 后，输入框保留 2，但标准草稿不会保存这个无效值；「继续故事规划」被禁用，底部却仍提示先确认总纲。直接打开原「快速创作」会采用 8 集预设，丢失这次输入的短集数意向。

主项目未确认总纲、无正文、无活动任务且无快速历史的大陆标准作品，显式输入 1–7 集后提供「按 N 集快速创作」。点击先保存当前原始资料，再进入带短集数意向的快速页；标准存储不会被自动保存为非法的 2 集。空想法可以先进入下一页补充，实际整理梗概仍由已有内容校验保护。

快速页先读取服务器，只在尚无快速状态且保护条件仍成立时采用该意向：N 集、约 N 千字、每集 90 秒。已有服务器状态优先；旧的未保存输入会保留恢复副本。打开入口不创建快速状态、不调用模型，后续由作者明确整理或确认梗概。

独立标准流程、海外作品、已有正文及受保护的已确认内容维持原规则。底部提示改为当前实际集数或下一步条件。

生产复现使用唯一临时 60 秒旧项目，在原始资料输入 2，确实看到原按钮禁用和错误总纲提示。修复只涉及剧本前端，API 与主站应用无需重启；实际发布与回查结果记录在下方。

前端全量 1416 项测试通过，包含源稿保存后导航、保存失败不跳转、短集数与字数传递、先读取服务器、已有状态优先、未保存缓存恢复以及空资料无模型调用等回归。生成模型未实际调用。

## 生产发布

2026-09-21 03:13:32（Asia/Shanghai）已发布至 https://xumutv.com。剧本 Web 业务提交 `dbb3386baced111e024a7b779a6267ca02bf84b5`，镜像 `script-master-web:dbb3386baced`。剧本 API 保持 `script-master-api:53364bb946e2`；主站 Web / API / Worker、两个数据库及 Redis 实例均保持不变，已核对容器 ID。

生产浏览器使用同一个临时旧项目复测：原始资料输入 2 后显示可用的「按 2 集快速创作」，点击成功进入「2 集 / 约 2000 字」页面，原故事想法保留，「使用快速创作，整理梗概」可用，控制台无错误。未点击付费生成；测试项目已在两侧归档，API 测试会话退出，独立验收标签页关闭，用户原页未改动。旧标签页需刷新加载新资源。

TypeScript、生产构建和默认非 root Web 文件可读检查通过，上传资料读取中禁用新入口的追加保护通过来源页 10 项回归。生产冒烟 `ok=true`，验证登录/项目隔离、快速状态恢复与幂等、正文交付、资产、规则分镜、修订历史和积分不变，无模型调用。

发布目录 `/opt/seqora-releases/script-short-entry-20260921`，构建与部署退出码均为 0。备份 `/opt/seqora-backups/script-short-entry-20260920T191308797362985Z`。回退仅恢复 Web 镜像 `script-master-web:53364bb946e2` 及上一源码指向，先检查在途请求，不重启 API、不覆盖数据库。后续文档提交不改变业务镜像。
