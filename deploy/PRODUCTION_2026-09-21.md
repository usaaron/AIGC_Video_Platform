# 快速剧本融合版生产发布

2026-09-21 00:29（Asia/Shanghai）已发布用户确认的本地 5182 融合版，生产入口为 https://xumutv.com。

剧本前后端业务提交均为 `5f41a27e48cfcf71fce6160f2af046a389f330d1`，镜像分别为 `script-master-web:5f41a27e48cf` 与 `script-master-api:5f41a27e48cf`。配套主站业务提交为 `ebb55fa3c7a9b8f81b784649cc5ee95a24af0e4f`。本次文档补充提交不改变业务镜像。

生产模型配置与 5182 对齐：50 个模型角色使用 `deepseek-v4-1-flash-260910`，保留 Astra 备用角色。服务器只读模型目录返回 200 并列出目标模型；模型凭据通过私密配置传递，没有放入源码或镜像。生产 PostgreSQL、签名票据和账号隔离配置保留。

前后端 Docker 构建、迁移及静态目录准备、readiness、登录保护、签名项目范围、快速设定持久化/恢复/幂等、正文交付与资产依据、5 项资产及 2 个规则镜头、明确返修与历史保留的生产检查均通过。临时项目已归档、测试会话退出、积分未变化。首次检查的 JSON 字段顺序误判已修正，并验证了自动回滚和重新发布。

新建主站大陆项目默认使用快速版。已有标准项目、海外路径和独立剧本大师保持对应流程；本次没有迁入本地示例数据库或演示鉴权。本地并行服务未重启。

本次没有模型生成或图片/视频任务，十集真实模型全流程验收仍未完成。完整服务版本、备份、日志与回退说明见 [主站生产发布记录](https://github.com/usaaron/AIGC_Video_Platform/blob/codex/quick-script-preview-20260920/docs/QUICK_SCRIPT_DEPLOYMENT_2026-09-21.md)。
