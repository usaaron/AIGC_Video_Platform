# 快速剧本、资产与分镜衔接

业务版本：Script `c2815746178df61d6f05245b29e650b2534cff5f`，Host `7358cd111a3dd5f55fa4a619831819ac25c9469e`。发布状态待最终线上验收记录补充。

快速版沿用一次创作安排确认。场景、物品的明确外观与固定细节随安排保存；正文声明每场实际出现的角色（包括无对白人物）、地点和关键物品。交付时用实际出场清单选取对应资料卡，未出场的计划物品不会创建资产建议。

主项目 v2 导入将实际正文的 SHA-256 与有序场次标题、原场次 ID 一起保存。资产页自动按规则读取资料，确认加入后可设计形象；分镜页执行生成时读取同一份清单。图片尚未设计的卡片也能关联到分镜，显示为“待设计”。普通资料刷新不覆盖已有分镜、图片或视频；正文修改沿用明确制作修订及历史保护。

海外台词保留英文与中文译文，其他创作资料保持中文。旧计划兼容没有场景/物品卡的情况；字段缺失、正文改动或场次依据失效时使用旧正文解析规则。旧 v1 delivery 路径不携带新增场次映射。

## 验证

- Quick 后端相关回归 113 项，其中新资产验证 16 项。
- 前端全量测试 1436 项通过；随后补充内外景字段的定向 46 项通过。类型检查、OpenAPI 同步检查通过。
- 中英文跨仓测试调用实际 producer、Host 导入、资产创建、分镜和 Web 参考选择代码：两集、4 张资产卡、4 个镜头，模型调用 0。
- 桌面及 390px CSS 视口完成资产自动读取、一次确认、分镜待设计标签检查；主项目内的创作安排卡片可展开查看。
- 候选镜像在隔离 PostgreSQL 完成存储 CAS、Quick 流程、Host 组织隔离、并发幂等、重启恢复和选择性制作修订验证。测试数据库/网络已清理。

所有样本为合成资料，未调用真实付费模型、图片或视频 Provider；不以此声称实际模型输出质量或全片制作已验收。

运行证据位于本地 `work/script-assets-pipeline-20260921/`；生产发布目录为 `/opt/seqora-releases/script-assets-pipeline-20260921`。部署使用精确 Git archive、双数据库备份、迁移检查、任务与连接门禁及应用回滚，数据库容器保持不变。
